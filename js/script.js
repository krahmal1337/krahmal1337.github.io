const i18n = {
  en: {
    title: "Nadeloader — free, open-source module loader",
    nav_features: "Features",
    nav_compare: "Compare",
    nav_faq: "FAQ",
    nav_download: "Download",
    hero_badge: "v2.0 is out — auto-updates built in",
    hero_title_1: "Free loader for",
    hero_title_2: "Neverlose, Skeet and other",
    hero_desc: "Nadeloader loads your cheats in seconds, watches for updates automatically, and keeps your profiles ready to go — no clutter, no extra windows. Completely free and open-source.",
    hero_cta: "Download",
    hero_compare: "See the difference",
    badge_free: "Free forever",
    badge_opensource: "Open source",
    badge_nowallet: "No payments",
    works_with: "Works with",
    compare_eyebrow: "Compare",
    compare_title: "See it side by side",
    compare_desc: "Drag the handle to compare standard vs. enhanced output.",
    compare_before: "После",
    compare_after: "До",
    features_eyebrow: "Features",
    features_title: "Everything you need, by default",
    features_desc: "No plugins, no extra setup, no payments — install and go.",
    f_instant_title: "Instant startup",
    f_instant_desc: "Modules load in parallel, skipping unnecessary checks on boot.",
    f_autoupd_title: "Auto-updates",
    f_autoupd_desc: "The latest build pulls itself in as soon as it's out — nothing to update by hand.",
    f_profiles_title: "Saved profiles",
    f_profiles_desc: "Keep multiple configurations and switch between them in one click.",
    f_logs_title: "Transparent logs",
    f_logs_desc: "A full launch log shows exactly what loaded and when.",
    f_light_title: "Lightweight",
    f_light_desc: "Under 5 MB with close to zero background footprint.",
    f_free_title: "Free & open source",
    f_free_desc: "No subscriptions, no paywalls, no hidden fees. MIT licensed.",
    cheats_eyebrow: "Compatible",
    cheats_title: "Works with your favorite cheats",
    cheats_desc: "Designed and tested with the most popular CS:GO cheat loaders.",
    cheat_nl: "Convenient script engine, built-in config manager, HvH.",
    cheat_sk: "Top-1 HvH cheat, legendary Lua API, max skill.",
    cheat_ot: "Old and outdated, but still supported for the remaining community.",
    faq_eyebrow: "FAQ",
    faq_title: "Common questions",
    faq_q1: "What cheats does this work with?",
    faq_a1: "Neverlose (convenient/HvH), Skeet (top-1 HvH), Onetap (old&outdated). Windows 10/11, .NET 8.",
    faq_q2: "Is this really free?",
    faq_a2: 'Yes. No subscriptions, no paywalls, no "premium" tiers. The project is MIT-licensed and always will be.',
    faq_q3: "How does auto-update work?",
    faq_a3: "On launch, nadeloader checks for new releases on GitHub and only downloads the files that changed.",
    faq_q4: "Can I contribute?",
    faq_a4: "Absolutely! Pull requests, issues, and feature suggestions are welcome. Check the GitHub repo to get started.",
    cta_title: "Ready to try it?",
    cta_desc: "Grab the latest build v3.0.2 — 4.6 MB, installs in 10 seconds.",
    cta_btn: "Download nadeloader.zip",
    cta_hash: "SHA-256 listed on the release page · Windows 10/11",
    footer_oss: "open source",
  },
  ru: {
    title: "Nadeloader — бесплатный хвх загрузчик с открытым кодом",
    nav_features: "Возможности",
    nav_compare: "Сравнение",
    nav_faq: "FAQ",
    nav_download: "Скачать",
    hero_badge: "Версия v2.0 — автоматические обновления",
    hero_title_1: "Бесплатный загрузчик для",
    hero_title_2: "Neverlose, Skeet и много другого",
    hero_desc: "Nadeloader инжектит читы за секунды, автоматически отслеживает обновления и хранит профили — без мусора и лишних окон. Полностью бесплатно и с открытым кодом.",
    hero_cta: "Скачать",
    hero_compare: "Посмотреть разницу",
    badge_free: "Навсегда бесплатно",
    badge_opensource: "Открытый код",
    badge_nowallet: "Не требует платежей",
    works_with: "Совместим с",
    compare_eyebrow: "Сравнение",
    compare_title: "Посмотрите рядом",
    compare_desc: "Перетащите ползунок, чтобы сравнить обычный и улучшенный результат.",
    compare_before: "После",
    compare_after: "До",
    features_eyebrow: "Возможности",
    features_title: "Всё необходимое из коробки",
    features_desc: "Никаких плагинов, настроек и покупок — установил и пользуйся.",
    f_instant_title: "Мгновенный запуск",
    f_instant_desc: "Читы загружаются параллельно, пропуская лишние проверки.",
    f_autoupd_title: "Автообновления",
    f_autoupd_desc: "Свежая сборка загружается сама, как только выходит — ничего не нужно обновлять вручную.",
    f_profiles_title: "Сохранённые профили",
    f_profiles_desc: "Храните несколько конфигураций и переключайтесь между ними в один клик.",
    f_logs_title: "Прозрачные логи",
    f_logs_desc: "Полный журнал запуска показывает, что и когда загрузилось.",
    f_light_title: "Лёгкий",
    f_light_desc: "Менее 5 МБ с почти нулевым потреблением в фоне.",
    f_free_title: "Бесплатно и открыто",
    f_free_desc: "Никаких подписок, paywall и скрытых платежей. Лицензия MIT.",
    cheats_eyebrow: "Совместимость",
    cheats_title: "Работает с вашими любимыми читами",
    cheats_desc: "Спроектирован и протестирован с популярными загрузчиками CS:GO.",
    cheat_nl: "Удобный скриптовый движок, встроенный конфиг-менеджер, HvH.",
    cheat_sk: "Топ-1 HvH чит, легендарный Lua API, максимальный скилл.",
    cheat_ot: "Старая залупа, но поддержка ещё есть для остатков комьюнити.",
    faq_eyebrow: "FAQ",
    faq_title: "Часто задаваемые вопросы",
    faq_q1: "С какими читами это работает?",
    faq_a1: "Neverlose (удобный/HvH), Skeet (топ-1 HvH), Onetap (старая залупа). Windows 10/11, .NET 8.",
    faq_q2: "Это действительно бесплатно?",
    faq_a2: "Да. Никаких подписок, paywall и «премиум»-тарифов. Проект распространяется по лицензии MIT и всегда будет бесплатным.",
    faq_q3: "Как работают автообновления?",
    faq_a3: "При запуске nadeloader проверяет новые релизы на GitHub и скачивает только изменившиеся файлы.",
    faq_q4: "Могу ли я участвовать в разработке?",
    faq_a4: "Конечно! Pull request'ы, issues и предложения приветствуются. Заходите на GitHub-репозиторий чтобы начать.",
    cta_title: "Готовы попробовать?",
    cta_desc: "Последняя сборка v3.0.2 — 4.6 МБ, установка за 10 секунд.",
    cta_btn: "Скачать nadeloader.zip",
    cta_hash: "SHA-256 указан на странице релиза · Windows 10/11",
    footer_oss: "открытый код",
  }
};

let currentLang = localStorage.getItem('nadeloader_lang') || 'en';

function applyLang(lang) {
  currentLang = lang;
  localStorage.setItem('nadeloader_lang', lang);
  const keys = i18n[lang];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.dataset.i18n;
    if (keys[key] !== undefined) {
      el.innerHTML = keys[key];
    }
  });
  document.documentElement.lang = lang;
  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.classList.toggle('active', btn.dataset.lang === lang);
  });
}

document.querySelectorAll('.lang-btn').forEach(btn => {
  btn.addEventListener('click', () => applyLang(btn.dataset.lang));
});

applyLang(currentLang);

document.getElementById('year').textContent = new Date().getFullYear();

/* loader */
window.addEventListener('load', () => {
  setTimeout(() => document.getElementById('loader').classList.add('hidden'), 400);
});

/* back to top */
const backTop = document.getElementById('backTop');
backTop.addEventListener('click', () => window.scrollTo({ top:0, behavior:'smooth' }));

const header = document.getElementById('header');
const navLinks = document.querySelectorAll('.nav a');
const glowA = document.getElementById('glowA');
const glowB = document.getElementById('glowB');

function updateNav(){
  let current = '';
  document.querySelectorAll('section[id]').forEach(s => {
    const top = s.getBoundingClientRect().top;
    if (top < 200) current = s.id;
  });
  navLinks.forEach(a => {
    a.style.color = a.getAttribute('href') === '#' + current ? 'var(--ink)' : '';
  });
}

window.addEventListener('scroll', () => {
  const sy = window.scrollY;
  header.style.boxShadow = sy > 8 ? '0 8px 30px rgba(0,0,0,0.35)' : 'none';
  header.style.background = sy > 8 ? 'rgba(11,4,23,0.85)' : 'rgba(8,9,12,0.7)';
  updateNav();
  backTop.classList.toggle('visible', sy > 400);
  if (glowA) glowA.style.transform = `translate(${sy * -0.02}px, ${sy * -0.015}px)`;
  if (glowB) glowB.style.transform = `translate(${sy * 0.02}px, ${sy * 0.015}px)`;
});
updateNav();

/* cursor glow */
const cursor = document.createElement('div');
cursor.className = 'cursor-glow';
document.body.appendChild(cursor);
document.addEventListener('mousemove', e => {
  cursor.style.transform = `translate(${e.clientX}px, ${e.clientY}px)`;
});

/* smooth anchor scroll */
document.querySelectorAll('a[href^="#"]').forEach(a => {
  a.addEventListener('click', e => {
    const id = a.getAttribute('href').slice(1);
    const el = document.getElementById(id);
    if (el) {
      e.preventDefault();
      el.scrollIntoView({ behavior: 'smooth' });
    }
  });
});

/* console easter egg */
console.log(
  '%c        __                  _           _           %c   \n' +
  '  _ __  / _|  ___   ___   __| |   ___   | | ___  ___  \n' +
  ' | \'__|| |_  / _ \\ / _ \\ / _` |  / _ \\  | |/ _ \\/ __| \n' +
  ' | |   |  _||  __/|  __/| (_| | | (_) | | |  __/\\__ \\ \n' +
  ' |_|   |_|   \\___| \\___| \\__,_|  \\___/  |_|\\___||___/ \n' +
  '                                                       \n' +
  '  Free • Open source • No bullshit                     \n',
  'color:#B84FE0;font-weight:bold;font-family:monospace;font-size:10px;line-height:1.2',
  'color:#9C87BE;font-family:monospace;font-size:10px;'
);

/* reading progress bar */
const progressBar = document.getElementById('progress-bar');
window.addEventListener('scroll', () => {
  const scrollTop = window.scrollY;
  const docHeight = document.documentElement.scrollHeight - window.innerHeight;
  const progress = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
  progressBar.style.width = progress + '%';
});

/* typewriter */
(function(){
  const el = document.getElementById('typing-text');
  if (!el) return;
  const text = el.textContent.trim();
  el.textContent = '';
  let i = 0;
  function type(){
    if (i < text.length){
      el.textContent += text.charAt(i);
      i++;
      setTimeout(type, 30 + Math.random() * 40);
    }
  }
  const obs = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting){
        setTimeout(type, 400);
        obs.unobserve(el);
      }
    });
  }, { threshold: 0.5 });
  obs.observe(el);
})();

/* particles */
(function(){
  const canvas = document.getElementById('particles');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  let w, h, particles = [];
  const COUNT = 60;

  function resize(){
    w = canvas.width = window.innerWidth;
    h = canvas.height = window.innerHeight;
  }
  window.addEventListener('resize', resize);
  resize();

  for (let i = 0; i < COUNT; i++){
    particles.push({
      x: Math.random() * w,
      y: Math.random() * h,
      vx: (Math.random() - 0.5) * 0.4,
      vy: (Math.random() - 0.5) * 0.4,
      r: Math.random() * 2 + 1,
      a: Math.random() * 0.3 + 0.05,
    });
  }

  function draw(){
    ctx.clearRect(0, 0, w, h);
    particles.forEach(p => {
      p.x += p.vx;
      p.y += p.vy;
      if (p.x < 0) p.x = w;
      if (p.x > w) p.x = 0;
      if (p.y < 0) p.y = h;
      if (p.y > h) p.y = 0;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(184, 79, 224, ${p.a})`;
      ctx.fill();
    });
    requestAnimationFrame(draw);
  }
  draw();
})();

/* magnetic buttons */
document.querySelectorAll('.magnetic').forEach(btn => {
  btn.addEventListener('mousemove', e => {
    const rect = btn.getBoundingClientRect();
    const x = (e.clientX - rect.left - rect.width/2) * 0.2;
    const y = (e.clientY - rect.top - rect.height/2) * 0.2;
    btn.style.transform = `translate(${x}px, ${y}px)`;
  });
  btn.addEventListener('mouseleave', () => {
    btn.style.transform = '';
  });
});

/* ripple effect on buttons */
document.querySelectorAll('.btn').forEach(btn => {
  btn.addEventListener('click', function(e){
    const rect = this.getBoundingClientRect();
    const ripple = document.createElement('span');
    ripple.className = 'ripple';
    const size = Math.max(rect.width, rect.height);
    ripple.style.width = ripple.style.height = size + 'px';
    ripple.style.left = (e.clientX - rect.left - size/2) + 'px';
    ripple.style.top = (e.clientY - rect.top - size/2) + 'px';
    this.appendChild(ripple);
    setTimeout(() => ripple.remove(), 600);
  });
});

/* 3D tilt on cards */
document.querySelectorAll('.feature-card, .cheat-card').forEach(card => {
  card.addEventListener('mousemove', e => {
    const rect = card.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width;
    const y = (e.clientY - rect.top) / rect.height;
    const tiltX = (y - 0.5) * -8;
    const tiltY = (x - 0.5) * 8;
    card.style.transform = `perspective(600px) rotateX(${tiltX}deg) rotateY(${tiltY}deg) translateY(-5px)`;
  });
  card.addEventListener('mouseleave', () => {
    card.style.transform = '';
  });
});

document.getElementById('downloadBtn').addEventListener('click', (e) => {
  e.preventDefault();
  alert(currentLang === 'ru'
    ? 'Замените это уведомление на ссылку на ваш релиз (например, GitHub Releases .zip).'
    : 'Replace this alert with a real link to your release (e.g. a GitHub Releases .zip / .exe).');
});

const revealEls = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale');
const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (entry.isIntersecting) {
      entry.target.classList.add('in-view');
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.15 });
revealEls.forEach((el) => revealObserver.observe(el));

const counters = document.querySelectorAll('.counter');
const counterObserver = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (!entry.isIntersecting) return;
    const el = entry.target;
    const target = parseFloat(el.dataset.target);
    const decimals = parseInt(el.dataset.decimal || '0', 10);
    const duration = 1400;
    const start = performance.now();
    function tick(now){
      const progress = Math.min((now - start) / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      const value = target * eased;
      el.textContent = decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');
      if (progress < 1) requestAnimationFrame(tick);
      else el.textContent = decimals ? target.toFixed(decimals) : target.toLocaleString('en-US');
    }
    requestAnimationFrame(tick);
    counterObserver.unobserve(el);
  });
}, { threshold: 0.5 });
counters.forEach((el) => counterObserver.observe(el));

const compareRange = document.getElementById('compareRange');
const compareClip = document.getElementById('compareClip');
const compareHandle = document.getElementById('compareHandle');
const compareSlider = document.getElementById('compareSlider');

function syncClipImageWidth(){
  const w = compareSlider.getBoundingClientRect().width;
  compareClip.querySelector('.compare-img').style.width = w + 'px';
}

function setCompare(value){
  compareClip.style.width = value + '%';
  compareHandle.style.left = value + '%';
}
syncClipImageWidth();
setCompare(50);
window.addEventListener('resize', syncClipImageWidth);

(function(){
  const baseText = currentLang === 'ru'
    ? 't.me/nademafia '
    : 't.me/nademafia ';
  const text = baseText.repeat(2);
  let pos = 0;
  const speedMs = 260;

  function tick(){
    document.title = text.slice(pos) + text.slice(0, pos);
    pos = (pos + 1) % baseText.length;
  }

  let timer = setInterval(tick, speedMs);

  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      clearInterval(timer);
    } else {
      timer = setInterval(tick, speedMs);
    }
  });
})();

compareRange.addEventListener('input', (e) => setCompare(e.target.value));

let dragging = false;
compareSlider.addEventListener('pointerdown', () => { dragging = true; });
window.addEventListener('pointerup', () => { dragging = false; });
compareSlider.addEventListener('pointermove', (e) => {
  if (!dragging) return;
  const rect = compareSlider.getBoundingClientRect();
  const pct = Math.min(Math.max(((e.clientX - rect.left) / rect.width) * 100, 0), 100);
  compareRange.value = pct;
  setCompare(pct);
});
